// This file is intentionally minimal - the actual layout is in [locale]/layout.tsx
// to support internationalization properly

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
