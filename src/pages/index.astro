---
/**
 * Legacy unprefixed home → English locale home.
 * Canonical English home is /en/
 */
import { DEFAULT_LOCALE, localizedPath } from "../i18n/locales";

return Astro.redirect(localizedPath(DEFAULT_LOCALE, "/"), 301);
---
